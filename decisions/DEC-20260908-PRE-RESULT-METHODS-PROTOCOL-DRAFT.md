# Pre-result Methods and protocol manuscript draft

## Decision

Add an English Methods and Experimental Protocol draft while the full-data
lanes run.  The draft is hash-bound to the executable paper protocol, frozen
theory bundle, manuscript branching contract, and current novelty boundary.

## Scope

The document covers only result-independent material: the UNSB sequential
state boundary, the three frozen operators and their pre-Adam properties, the
data/sampler/optimizer protocol, exact recovery rules, runtime-scoped matched
controls, fixed evaluation, and reporting limitations.

It is explicitly subordinate to the machine-readable authorities.  It does
not select an algorithm, claim full-data benefit, open confirmation20, or add a
new experiment.  Empirical language can enter the manuscript only through the
committed post-result claim-freeze path.

## Critical-path effect

This moves substantial writing work off the post-e200 critical path without
using any in-flight paired result.  Training processes, queues, protocols,
checkpoints, and recovery chains are unchanged.

The authoritative receipt is
`evidence/paper_aio/PAPER_AIO_PRE_RESULT_METHODS_PROTOCOL_DRAFT_20260908T065000.json`.
