# Goal heartbeat dynamic authority

The active heartbeat must fetch and read `origin/main` on every wake. A Git commit embedded in an earlier prompt is only historical context and must never override the current repository authority.

The heartbeat now also treats DCLGAN source-export children as dynamic. Supervisor state and the child state file are authoritative; absence of retired PID `20760` must not trigger recovery while the current child and supervisor remain healthy.

The two-hour cadence and failed-run-only notification policy are unchanged. No training, checkpoint, metric or confirmation20 state was modified.

Compact evidence: `evidence/paper_aio/PAPER_AIO_GOAL_HEARTBEAT_DYNAMIC_AUTHORITY_20260913T195000.json`.
