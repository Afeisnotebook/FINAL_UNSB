# Final discovery portfolio V10 closed

Final V9 failed after all training and fixed evaluations had completed because the portfolio builder required ST-CGR inside the immutable five-lane first-wave aggregate while also binding ST-CGR through its independent disposition. This was a control-contract contradiction, not a training or algorithm failure.

Commit `d99e4ca906313f16607d25474432eb77cf4b6b4b` removes that duplicate requirement. Forty-five focused tests passed locally and on the frozen 4090 evaluation runtime. Fresh, isolated Final V10 and DCLGAN addendum V4 chains then completed naturally under durable supervisors. No training checkpoint, metric implementation or evaluation bundle was changed.

The complete discovery portfolio accepts Proposal-only under the preregistered full-data gate: its e150/e175/e200 macro-PSNR delta mean is `+1.724 dB` and its fixed e200 delta is `+0.839 dB` against exact-runtime matched plain. The current full-data ST-CGR and AM-TNC implementations fail their registered gates (`-0.692/-2.744 dB` and `-2.108/-4.072 dB` for late-three/e200 respectively). These are current-implementation results, not family-level falsifications. HJCGR remains deferred and DDSB reproduction remains incomplete.

CUT, CycleGAN and DCLGAN are complete external baselines. DCLGAN remains a standalone fixed-protocol comparator and is not assigned an illegal matched delta. The terminal pathology audit did not support adding a terminal repair module.

Discovery is therefore complete enough for an explicit claim-freeze and confirmation decision, but claims are not yet frozen and confirmation20 remains sealed. The next action is a human-visible scientific review of the complete portfolio and disclosure boundaries; only after that review may the pre-existing confirmation policy be authorized.
