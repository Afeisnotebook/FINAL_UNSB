# 5090B clone e131 continuation and final relay rebind

The provider-cloned 5090B is not a replacement experiment. It is an admitted,
segmented exact-runtime continuation of the existing matched-plain lane. The
clone passed the 2000-update runtime twin, two independent eight-update resume
probes produced the same full and scientific states, and the preserved source
checkpoint at step 992148 was not overwritten. The live lane has since reached
e131 / 1,120,443 updates with zero supervisor or continuation-guard restart.

The stopped provider endpoint exposed one remaining engineering gap: the local
final export relay still targeted port 44804. It was retired without touching
training, checkpoints, or existing evidence. A source-bound relay now targets
the clone at port 43172 and is protected by a recovery supervisor and health
watch. The already deployed incremental local relay and the 4090A unified
evaluation relay were independently confirmed to target 43172 and remain
healthy. CUT and CycleGAN are already complete with source-bound e200 exports.

The Windows full-relay recovery supervisor previously enumerated Linux
`/proc` only. Commit `f412083` adds exact command discovery through the pinned
Windows `psutil` runtime, preserving fail-closed command matching. Sixteen
targeted tests pass. This is an engineering delivery repair, not an algorithm,
optimizer, data-order, or runtime-cohort change.

The matched-plain lane is projected to reach e200 around late September 15 to
early September 16 (UTC+8). Keep the clone available through final export and
hash verification. No paired performance value was read, confirmation20 stays
sealed, and no other healthy training was interrupted.
